// Firestore Rules (dev-only; restricted for prod!)

rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /incidents/{id} {
      allow read, write: if request.auth != null;
    }
  }
}
